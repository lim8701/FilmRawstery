# Python
__pycache__/
*.py[cod]

# 가상환경 (용량 큼, requirements.txt 로 재생성)
.venv/

# 근사 LUT 백업 (make_luts.py 로 재생성 가능)
luts/_approx_backup/

# ONNX 모델 (용량 큼, 최초 실행 시 자동 다운로드 — 출처/링크는 models/README.md 참조)
models/*.onnx
# Florence-2 캡션 모델 일체(onnx + 토크나이저/설정 json — caption.ensure_model 이 다운로드)
models/florence2_*
# AI 디노이즈 GPU 디바이스 프로빙 캐시(머신 전용 — GPU 구성 변경 시 삭제하면 재프로빙)
models/ai_denoise_device.json

# 흑백 필름시뮬 LUT (Stuart Sowerby, All Rights Reserved — 재배포 불가): 로컬 사용만, 커밋 금지.
# 동적 목록이라 파일만 있으면 앱에 자동 노출됨(없으면 숨김).
luts/acros.cube
luts/acros_g.cube
luts/acros_r.cube
luts/acros_ye.cube
luts/monochrome.cube
luts/sepia.cube

# 패키징 산출물 (FilmRawstery.spec 빌드 레시피는 추적 유지)
/build/
/dist/
*.zip

# OS
Thumbs.db
.DS_Store
